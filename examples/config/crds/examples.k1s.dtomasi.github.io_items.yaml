---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: items.examples.k1s.dtomasi.github.io
spec:
  group: examples.k1s.dtomasi.github.io
  names:
    kind: Item
    listKind: ItemList
    plural: items
    shortNames:
    - itm
    singular: item
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.name
      name: Item Name
      type: string
    - jsonPath: .spec.quantity
      name: Quantity
      type: integer
    - jsonPath: .spec.category
      name: Category
      type: string
    - jsonPath: .status.status
      name: Status
      type: string
    - jsonPath: .spec.price
      name: Price
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Item is the Schema for the items API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ItemSpec defines the desired state of Item
            properties:
              category:
                description: Category is the name of the category this item belongs
                  to
                minLength: 1
                type: string
              description:
                description: Description provides details about the item
                maxLength: 500
                type: string
              name:
                description: Name is the display name of the item
                maxLength: 100
                minLength: 1
                type: string
              price:
                description: Price is the cost of the item in cents
                format: int64
                minimum: 0
                type: integer
              quantity:
                default: 1
                description: Quantity is the number of items available
                format: int32
                minimum: 0
                type: integer
            required:
            - category
            - name
            - quantity
            type: object
          status:
            description: ItemStatus defines the observed state of Item
            properties:
              lastUpdated:
                description: LastUpdated is the timestamp when the item was last modified
                format: date-time
                type: string
              reservedBy:
                description: ReservedBy indicates who has reserved this item
                type: string
              status:
                default: Available
                description: Status represents the current state of the item
                enum:
                - Available
                - Reserved
                - Sold
                - Discontinued
                type: string
            required:
            - status
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
